ADD_CUSTOM_COMMAND(VERBATIM
	DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/maintain.tex ${CMAKE_CURRENT_SOURCE_DIR}/maintain.sty"
	OUTPUT "${CMAKE_CURRENT_SOURCE_DIR}/maintain.dvi"
	COMMAND "latex saclib.tex && latex saclib.tex"
	WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
)
